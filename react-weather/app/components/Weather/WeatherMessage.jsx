var React = require('react'),

    WeatherMessage = React.createClass({
      render: function() {
        return(
        <p>Weather Message Here</p>
        );
      }
    });

module.exports = WeatherMessage;
